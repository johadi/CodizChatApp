import React from 'react';
import ReactDOM from 'react-dom';

const App = () => (
    <div>
      <h2>The React App works!</h2>
    </div>
);

ReactDOM.render(<App/>, document.querySelector('#app'));
